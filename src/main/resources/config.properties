# Path to the directory where lock files are stored
lock.path=/path/to/locks

# Timeout in milliseconds for attempting to acquire the lock
lock.timeout=5000

# Maximum number of retries for the task execution
max.retries=3

# Delay in milliseconds between retries
retry.delay=1000
